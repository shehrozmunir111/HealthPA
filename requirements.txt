# FastAPI & Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Validation & Serialization
pydantic==2.11.9                  # bumped for langchain-pinecone (needs >=2.11.1)
pydantic-settings==2.14.1         # pulled by ragas
email-validator==2.1.0

# Async & Background Tasks
celery==5.3.6
redis==5.0.1

# OCR & AI
pytesseract==0.3.10
Pillow==10.2.0
openai>=1.58.1,<2.0.0             # bumped for langchain-openai compatibility (AsyncOpenAI API stable)

# AI Layer — LangGraph / LangChain (grounded coding, HITL, agents)
# Ranges let pip resolve a mutually compatible set (langchain-pinecone vs
# langchain-tavily disagree on aiohttp at older pins); frozen after install.
langchain-core==0.3.86            # bumped by ragas
langgraph==0.2.74
langgraph-checkpoint-postgres==2.0.21
psycopg[binary]==3.2.3
langchain-openai==0.3.28          # LM Studio (OpenAI-compatible) chat + nomic embeddings
langchain-groq==0.2.5             # cloud chat option
langchain-pinecone==0.2.13        # vector store (prod); uses pinecone v6
pinecone==6.0.2
langchain-tavily==0.1.6           # web search tool for policy-QA agent
langsmith==0.3.45                 # optional observability (env-gated)
pypdf==5.1.0                      # policy PDF ingestion
ragas==0.2.15                     # RAG/extraction evaluation (faithfulness, recall, etc.)

# AWS SES
boto3==1.34.34

# Utilities
python-dateutil==2.8.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.28.1                    # bumped for langchain-pinecone (needs >=0.28.0)
